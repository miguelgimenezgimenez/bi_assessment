const clientId = process.env.CLIENT_ID
const clientSecret = process.env.CLIENT_SECRET
const apiUrl = process.env.API_URL

class ApiService {
 
  login() {
    
  }
}


module.exports = new ApiService(clientId, clientSecret)